{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window and project windows",
  "windows": ["main", "project-*"],
  "permissions": [
    "core:default",
    "dialog:default",
    "opener:default"
  ]
}
